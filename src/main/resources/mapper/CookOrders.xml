<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.foodsite.dao.CookOrderDAO">

<select id="cookorderList" resultType="cook_order">
    select cook_order_id, cook_order as 'order', cook_image, description, recipe_id
    from cook_order
    where recipe_id = #{recipe_id}
    order by cook_order asc
</select>


</mapper>